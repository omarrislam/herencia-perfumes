# Decisions Log

Locked decisions. **Do not re-litigate** without explicit user change. Newest at bottom.

_2026-06-29_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 1 | Language | English only (LTR) | User choice; drops i18n/RTL complexity |
| 2 | Currency | EGP | User confirmed; market is Egypt |
| 3 | Checkout | COD + WhatsApp order capture, no gateway | Lightweight starter; COD common in Egypt |
| 4 | Catalog | Perfumes + bundles | User choice |
| 5 | Rendering/SEO | Option A — SSR-lite (SPA + server-injected meta + prerendered static) | Balances SEO, performance, "lightweight" |
| 6 | Hosting | Single VPS (Node serves API + web) | User deferred; fits Option A, cheap |
| 7 | Auth | JWT in httpOnly cookie; roles customer/admin | Secure, simple |
| 8 | Images | Cloudinary (WebP/AVIF, responsive) | Performance + image quality |
| 9 | Animations | Framer Motion + CSS, lazy, prefers-reduced-motion | Engaging but perf-safe |
| 10 | Styling | Tailwind CSS + CSS-var brand tokens | Tiny output, fast, theme-able |
| 11 | Dark mode | Yes, `data-theme` | Required by user |
| 12 | Monorepo | npm workspaces (web/api/shared) | Light; shared types; no Nx/Turbo |
| 13 | Phasing | Build everything in phase 1 (incl. quiz + blog) | User choice |
| 14 | State persistence | Update memory + TASKS at every checkpoint | User requirement |
| 15 | Fonts | Cinzel (display) + Jost (body/UI) | Elegant heritage pairing, perf-friendly variable font |
| 16 | Images | Cloudinary confirmed by user | Performance + image quality |
| 17 | Spec | Approved by user 2026-06-29 | Ready for implementation planning |

_2026-06-30 (Milestone 1)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 18 | Admin auth (M1 interim) | `x-admin-token` header checked against `env.ADMIN_TOKEN` via `requireAdmin(token)` middleware; web stores token in `sessionStorage` behind an `AdminTokenGate` | Unblocks admin CRUD before the M2 auth system. **M2 replaces ONLY the middleware internals** (JWT httpOnly cookie + role check per decision #7) — route definitions and the gate seam stay identical. |
| 19 | SSR-lite implementation | Request-time `<head>` injection (server reads built `index.html`, strips `<title>`, injects per-route meta + OG + JSON-LD before `</head>`) + `/sitemap.xml` + `/robots.txt`. Build-time static prerender deferred to M4 perf pass. | Delivers per-route SEO now with minimal complexity; satisfies decision #5 Option A. |
| 20 | API test harness | `mongodb-memory-server`, with vitest `fileParallelism: false` + model `init()` index prebuild for determinism; on Windows, mongod temp redirected off the (full) C: drive via `MONGOMS_TMPDIR` (win32-only, env-overridable). | No external DB in tests; serialized to avoid parallel-mongod contention. Temp redirect is a local-machine workaround — revisit for CI/VPS (see next-session). |

_2026-07-01 (Milestone 2)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 21 | Auth (real, replaces #18 interim) | JWT HS256, 7-day, in an httpOnly + SameSite=Lax + Secure-in-prod cookie named `herencia_token`; bcryptjs cost 12; `jsonwebtoken` lib; secret from `process.env.JWT_SECRET` (≥16). `requireAdmin` seam kept but internals = `authenticate`+`requireRole('admin')`; `ADMIN_TOKEN`/`x-admin-token`/`AdminTokenGate` removed. | Fulfils decision #7; admin route definitions unchanged per #18; the interim header path is fully retired (no dead code). |
| 22 | Cart pricing | Single `priceItems(items)` service is the authoritative price source, consumed by both the cart endpoints and order creation. Client only ever sends `{productId,sizeLabel,qty}`; server recomputes every line/total from the DB. Guest cart in `localStorage['herencia.cart']`; logged-in cart server-persisted (`Cart` model) and merged once on login. | "Never trust client prices" — one place to recompute; guest→account continuity. |
| 23 | Order creation | COD only, status starts `pending`. `createOrder` re-prices via `priceItems`, rejects on unavailable/empty, then atomic conditional stock decrement (`$elemMatch stock>=qty` + positional `$inc`) with rollback on any decrement miss AND on post-loop persistence failure. orderNumber `HRC-<base36 ts>-<rand>`. WhatsApp capture link built server-side from `Setting.whatsappNumber`. No multi-doc transaction (YAGNI; documented tradeoff). | Stock integrity without overselling; lightweight COD flow per spec. |
| 24 | Order status lifecycle | `ORDER_STATUS_TRANSITIONS` map governs legal moves (pending→confirmed/cancelled, confirmed→shipped/cancelled, shipped→delivered, delivered/cancelled terminal). Admin PUT enforces it (422 on illegal); the admin UI `<select>` only offers current+legal next states. | One source of truth for the lifecycle, enforced server- and client-side. |
| 25 | Error envelope | `HttpError` gained optional `details?`; `errorHandler` emits `{error:{message,code,details?}}`, including it only when present (backward-compatible). Used by the `cart_unavailable` 409 to return which lines failed. | Matches the spec's `details?` contract without breaking existing responses. |

_2026-07-01 (Milestone 3)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 26 | Reviews | One review per user per product (compound unique `{product,user}` + `Review.exists` pre-check → 409). Public GET returns APPROVED only; POST creates `isApproved:false`. `Product.rating` (`{avg,count}`, avg 1-dp) recomputed from APPROVED reviews on every admin approve/unapprove/delete via `recomputeProductRating`. | Moderated, spam-bounded ratings with a denormalized aggregate kept correct across the lifecycle. |
| 27 | Quiz | Answer **weights are server-only**: public `GET /api/quiz` returns labels via `toQuizQuestionPublicDTO` (weights structurally absent); `POST /api/quiz/result` accumulates weights server-side → top scent-family/gender → recommended active perfumes (fallback: top-rated). Admin CRUD carries weights via `toQuizQuestionAdminDTO`. No QuizResult persistence (analytics-only, YAGNI). | Prevents gaming the quiz; keeps the recommendation logic server-authoritative. |
| 28 | Banners | Public `GET /api/banners?placement=` returns only `isActive` banners within their schedule window (`startsAt`/`endsAt` absent-or-in-range); admin sees all. `datetime-local` inputs convert to/from ISO with the local-timezone offset (not raw UTC). CTA links render internal via `<Link>`, external via `<a rel=noopener>` restricted to `http(s)/mailto/tel` schemes. | Scheduled promos; correct local scheduling; no `javascript:`-scheme injection on the public page. |
| 29 | Blog | Public list/detail return PUBLISHED only (drafts never leak to list/detail/sitemap/SEO meta); `publishedAt` set on first publish and preserved on re-edit. Body stored raw and rendered as escaped split-on-blank-line `<p>` (NO markdown lib / NO `dangerouslySetInnerHTML` — real markdown deferred to M4). SEO: per-post `<head>` + **Article** JSON-LD (`<`-escaped) via the M1 `routeMetaForPath` seam; published slugs in `sitemap.xml`. | SEO-ready blog with zero stored-XSS surface; markdown is a later enhancement. |
| 30 | Rate-limiting | Still deferred to the M4 security pass (reviews are auth-gated + one-per-user, so abuse is bounded). | YAGNI; consistent with M2. |

_2026-07-02 (Milestone 4 — Polish, code)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 31 | Blog markdown | Body now rendered as **real markdown** via `marked` → sanitized with **DOMPurify** (`renderMarkdownSafe` in `apps/web/src/lib/markdown.ts`); `dangerouslySetInnerHTML` permitted **only** on DOMPurify output. Supersedes #29's escaped-`<p>`. | Real formatting for the journal while keeping zero stored-XSS surface (3 load-bearing sanitization tests). |
| 32 | CSP | Real Helmet CSP in `apps/api/src/app.ts`: `default-src 'self'`; `script-src 'self'` (no unsafe-inline/eval); `style-src 'self' 'unsafe-inline'` (Framer inline styles); `img-src 'self' data: https://res.cloudinary.com`; `font-src/connect-src 'self'`; `object-src 'none'`; `base-uri 'self'`; `frame-ancestors 'none'`; `upgradeInsecureRequests` prod-only. | Resolves M1 deferred F-min-3; locks the public surface without breaking the SPA/Cloudinary/Framer. |
| 33 | Rate-limiting (resolves #30) | `express-rate-limit` limiters (`makeLimiter` + `authLimiter`/`orderLimiter`/`reviewLimiter`) on auth login+register, order POST, review POST; **skip when `NODE_ENV==='test'`**; 429 `{error:{message,code:'rate_limited'}}`. MemoryStore (fine for single-VPS). | Bounds abuse on the write paths; test-skip keeps the suite green. |
| 34 | Static prerender (fulfils #19/#5 Option A) | Build-time `renderToString` prerender of shell routes (`/`, `/find-your-scent`, `/login`, `/register`, `/blog`) → `dist/<route>/index.html`; client `hydrateRoot` when `root.hasChildNodes()` else `createRoot`. Router refactored to a shared `routes[]` config consumed by `BrowserRouter`+`useRoutes` (client) and `StaticRouter` (prerender); `StorefrontLayout` inner `<Suspense>` around `<Outlet>` so lazy pages degrade to their fallback while the chrome prerenders. API request-time `<head>` injection stays the SEO source. | Faster first paint of static chrome without a full SSR runtime; contexts made SSR-safe with minimal `typeof window` guards. |
| 35 | Motion | Framer Motion (lazy via consumers), `apps/web/src/lib/motion.ts` tokens (`DURATION`/`EASE_OUT`/`fadeUp`), `useReducedMotion`, `Reveal`. Transforms/opacity only; `prefers-reduced-motion` honored everywhere (reveals, button press, card hover, cart drawer slide, logo shimmer, route cross-fade). | Engaging but perf-safe motion; content never hidden when motion is skipped. NOTE: framer lands in the entry chunk (StorefrontLayout route cross-fade + cart drawer are eager) — the main lever if mobile Lighthouse needs trimming. |
| 36 | Fonts | Self-hosted Cinzel/Jost via `@fontsource/*` (7 weights, `font-display: swap`), Google Fonts `<link>`/preconnect removed. Family names unchanged (match Tailwind). | Eliminates the render-blocking third-party font request. |

_2026-07-07 (Post-M4 round 8 — sales-first UX)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 37 | Home layout | Sales-first default section order: `featured` (products) directly under the hero, then `samples` (ThreeSteps is now a reorderable CMS section), then editorial. Stored `sectionOrder` without `'samples'` is treated as unmigrated → serve the new default until admin re-saves. Hero image/text render only after settings resolve (espresso placeholder, fixed height) so the swirl fallback never flashes before an admin hero. | User wants conversion-first UX; kills the wrong-hero flash. |
| 38 | Samples | Per-unit sample product (slug `sample-box` migrated by `ensureSampleBox`: name "Perfume Sample", one `2ml` size, default 60 EGP, price **admin-editable in Admin → Products**). Cart line qty = number of picked samples; picked names go into order notes + shown in cart UI from SampleContext. Card CTA is "Order a sample"; modal preselects the clicked product (green success tokens), prominent +/✓ controls, footer totals N × unit. `SAMPLE_BOX` constant is only the seed/migration default. | "Add only the specified samples", editable pricing, one product keeps cart/stock/pricing untouched. |
| 39 | Email discount popup | `settings.emailPopup {enabled,title,text,code,discountPercent}` (Admin → Home). `Subscriber` model + public rate-limited `POST /api/newsletter` (idempotent, returns code). Popup (5s delay, 7-day re-dismiss, never on cart/checkout/confirmation/admin) stores the code in `localStorage['herencia.discountCode']`; checkout auto-applies it. **Server-side validation only**: `createOrder` recomputes `discount` from settings (`discount`/`discountCode` persisted on Order + DTO); client math is preview-only. Code is public in the settings DTO (it's a marketing code). | Real discount, never trusts client amounts, YAGNI (no per-email codes). |
| 40 | Checkout/confirmation | Checkout is a compact 2-col (form + sticky summary), no address line 2 (schema keeps `line2` optional for old data), payment cards only — **no InstaPay details at checkout**. COD confirmation = confirmed + "4–5 business days", WhatsApp is optional contact (no required confirmation step). InstaPay confirmation shows `settings.instapay.payLink` (admin-editable link; QR UI removed, model field kept) + WhatsApp screenshot step. | Fewer steps to buy; guest checkout already supported. |

_2026-07-07 (Post-M4 round 12 — homepage trim, admin order details/delete, hero LCP)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 41 | Home sections | `craft` section key REMOVED app-wide (shared schema/enum/defaults, Setting model, serialize, AdminHome, Home) — essence + atelier merged into one image-led `essence` split ("Composed by hand. Matured for months. Bottled in small batches."). Stored sectionOrders self-heal via `normalizeSectionOrder` (unknown keys dropped). | Two sections said the same thing across two full screens; one split removes a whole scroll. |
| 42 | Admin order delete | `DELETE /api/admin/orders/:id` → 204, permanent removal, **does NOT restore stock** (same semantics as cancelling — no stock restore anywhere in the status lifecycle). UI: click row → expanded details panel + Delete with `window.confirm`. | Consistent with existing cancel behavior; delete is an admin cleanup tool, not a returns flow. |
| 43 | Hero LCP | Hero renders a plain `<img>` with the EXACT preloaded URL (`cld(image,{w:1600})`, single width, no srcset) + `fetchpriority=high`; splash hold 1100→400ms. | ProductImage's srcset (400/800/1200) picked a different width than the w=1600 preload → preload wasted + late hero download. One canonical URL guarantees the preload is consumed. |

_2026-07-09 (Post-M4 round 16 — fulfillment flow hardening)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 44 | Phone validation | Shared `egyptianPhoneSchema` on order customer + shipping phones: normalizes `+20`/`20`-prefixed and spaced/dashed input to local `01[0125]XXXXXXXX`; rejects everything else with a friendly message. Checkout shows per-field zod errors (mapped by issue path, focus first invalid, typing clears). | Phone is the only recovery channel for COD; kills junk/fake orders and typos at the source. |
| 45 | Cancel restores stock (revises #42) | Admin status transition → `cancelled` increments each item's size stock back (fail-soft if product/size deleted). DELETE still never restores stock — cancel first if units should return to the shelf. | COD refusals are routine in Egypt; every cancel was silently eating inventory. |
| 46 | InstaPay payment tracking | `Order.paidAt` (Date) + `PUT /api/admin/orders/:id/paid {paid:boolean}` (`updateOrderPaidSchema`). Admin list shows a Payment badge column (COD / InstaPay · unpaid / InstaPay · paid); advancing an UNPAID InstaPay order to confirmed/shipped asks for confirm (soft guard, not blocked). Receipt banner: "COLLECT ON DELIVERY: EGP X" (COD) / "PAID VIA INSTAPAY — DO NOT COLLECT" / "INSTAPAY — PAYMENT PENDING". | Unpaid InstaPay orders were indistinguishable from COD; the courier line makes collection mistakes impossible. |
| 47 | Order status timestamps | `Order.statusHistory [{status,at}]` seeded `[pending]` on create, appended on every real transition; in the DTO; admin details shows a Timeline. Pending orders >24h get a `waiting Xh/Xd` badge + warning-tinted row. Notes cap 500→2000 (uncapped samples list lives in notes). Admin orders list gets `?q=` search (order no. / name / phone, regex-escaped) + pagination UI (API already paginated at 20 — the UI silently showed only page 1 before). | "How long has this been pending" is the core fulfillment signal; search unblocks "where's my order" calls. |

_2026-07-17 (Post-M4 round 35 — WhatsApp customer notifications)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 48 | Customer WhatsApp notifications = **wa.me deep links, not the Cloud API** | Admin → Orders → expanded order has **WhatsApp receipt** + **WhatsApp "<status>" update** links (`apps/web/src/features/admin/whatsappMessage.ts`): opens WhatsApp pre-filled at the customer, owner taps send. Copy mirrors the dormant `waCloud.ts` templates. `waCloud.ts` KEPT and still env-gated (dead unless `WA_PHONE_NUMBER_ID`/`WA_ACCESS_TOKEN` ever get set). | Meta portfolio `1401383105229630` is **permanently restricted** ("prohibited from advertising, including app sharing", 11 Jul 2026, review returned final) → a WABA can never be provisioned. Rebuilding under the clean portfolio `1647810982950230` = circumvention, risks that portfolio + the personal account. Unofficial libs (Baileys/whatsapp-web.js) need an always-on process the Vercel API can't host + risk banning the store's real number. wa.me is free, reaches everyone (phone required), zero platform risk; cost is one tap per order — accepted at current volume. wa.me cannot auto-send by design. |
| 49 | If automation ever beats channel | Escalate to **email** (only reaches the subset who supplied one — `email` is `.optional()` on `createOrderSchema`) or an **Egyptian SMS gateway** (reaches everyone, per-message cost). Not WhatsApp. | Neither needs Meta's permission; both can actually be automatic. |

_2026-07-29 (Post-M4 round 37 — pre-launch gap fixes)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 50 | Admin catalog reads its own endpoint | New `GET /api/admin/products` (paginated, `q` filter) returns products **without** the `isActive` filter; AdminProducts/AdminInventory use it, AdminDashboard takes catalog + stock counts from `/api/admin/stats` (new `products`/`lowStock`/`outOfStock`, aggregated over the whole catalog incl. inactive + sample pools). Public `GET /api/products` still filters `isActive: true`. | Admin screens read the PUBLIC catalog, so unchecking "Active" made a product vanish from the only UI that could switch it back on — unrecoverable without DB access. Also retires the silent 48-item cap on Products/Inventory/Dashboard. |
| 51 | Guest order lookup | Public `POST /api/orders/track` (order number **+** the phone it was placed with, own `trackLimiter` 20/15min) + `/track` page, linked from the footer, the confirmation page (`?order=` prefill) and the "no order to display" fallback. One 404 message for both wrong-phone and unknown-order. | Most orders are guest orders and nothing is sent automatically (decision #48), so the confirmation tab was the customer's only record. Requiring the phone stops an order number alone from exposing an address; a single 404 message avoids confirming which numbers exist. |
| 52 | Discount codes are one per phone number | The email-popup code is honoured only when that phone has **no** prior non-cancelled order (it's a first-order offer); admin codes only when that phone hasn't used **that** code before. Rejection is a loud 409 `discount_not_eligible`, not a silent drop. Discount now resolves **before** stock is decremented, and a code's `uses` counter only increments **after** the order exists. Checkout always shows an applied code with a **Remove** control. | "10% off your first order" was 10% off every order forever for anyone. Cancelled orders don't burn the offer (COD refusals are routine). Loud rejection because checkout has already shown a discounted total — and the customer needs a way to clear the code, which the applied state used to hide. |
| 53 | Delete requires cancel first (revises #42) | `DELETE /api/admin/orders/:id` 409s (`cancel_before_delete`) unless the order is already `cancelled`; the admin button is disabled with the reason inline. | Cancelling restores stock and deleting never did, so deleting a live order silently ate inventory — the instinctive action was the destructive one. |
| 54 | Stale unpaid InstaPay = owner-triggered sweep | `GET /api/admin/orders/stale-unpaid?hours=` counts unpaid pending InstaPay orders older than N hours (default `STALE_UNPAID_HOURS` 48); `POST /api/admin/orders/release-stale` cancels them, restoring stock. Surfaced as a banner + confirm button on Admin → Orders. **No cron** — nothing auto-cancels a real customer's order. | InstaPay decrements stock at creation and stays pending forever if nobody pays. Rejected the scheduled job: it would cancel real orders unattended and needs deploy config. |
| 55 | Guest orders adopt into accounts | `linkGuestOrders(userId, {email, phone})` on register, login and profile save; matches `user: null` orders by case-insensitive email or normalized Egyptian phone. Never reassigns an owned order. | An order placed logged-out was invisible in the account forever. The phone usually only arrives with the profile, so login alone isn't enough. |
| 56 | Low-stock push | After an order decrements stock, any size (or sample pool) at or below `LOW_STOCK_THRESHOLD` (5) fires one ntfy alert (priority 3, click → `/admin/inventory`). Stock decrement switched to `findOneAndUpdate({new:true})` so the remaining count is known without a second read. Shares one fail-soft `publish()` helper with the new-order alert. | Low stock was dashboard-only — the owner had to go look. Same env-gating and fail-soft rules as decision-27-era ntfy work; a notification failure must never break checkout. |

_2026-08-14 (Post-M4 round 38 — launch prep)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 57 | Launch reset is a script, dry-run by default | `apps/api/src/scripts/resetLaunch.ts` + `.cli.ts` (`npm run reset-launch -w apps/api`). Deletes orders, carts, reviews, subscribers; zeroes `Product.rating` and `DiscountCode.uses`. Destructive run requires `--yes-wipe-production`; `--delete-test-accounts` additionally removes customer accounts on `example.com` only. Never touches catalog, settings, scent families, quiz, banners, blog, or admins. | `seed.ts` sits next to it and wipes products + settings — a footgun on a live store, so the reset had to be a separate, narrower tool. `example.com` is RFC-2606 reserved and can never be a real customer, making it the one email rule safe to delete on. A `listCollections` before/after test guards against a future edit widening the blast radius. **Always list the rows, not just counts, before running: the 2026-08-14 dry run surfaced an order that looked like a real customer.** |
| 58 | Bottle size is stated, but only when unambiguous | `soleSizeLabel()` returns a label only for single-size products. Used on ProductCard, the PDP, the SEO title (`Name — 55ml EDP — HERENCIA`) and `"size"` in Product JSON-LD. Multi-size products state no size and keep their "from" price. | The size existed as data but appeared nowhere: `ProductDetail` gated the size block on `sizes.length > 1`, so single-size perfumes rendered none at all. Naming one size for a multi-size product would be actively wrong, so the helper is the gate everywhere. Size in the title targets the real long-tail query ("55ml perfume"), not the bare product name. |
| 59 | Per-route SEO is baked at build time | `GET /api/seo/prerender` returns `{path, head}` for static routes + active products + published posts; `apps/web/scripts/bake-seo.mjs` writes `dist/<route>/index.html` with the head injected, plus `sitemap.xml` + `robots.txt`. `lib/seo.ts` remains the only SEO logic — the build reimplements nothing. Deploy order matters: **api first, then web.** | On the split Vercel deploy the web project serves prebuilt HTML and never routes through the API, so `mountSpa`'s request-time injection never ran and every page shipped an identical `<title>` (the round-28 known gap). No `vercel.json` change needed — Vercel checks the filesystem before applying rewrites, so baked files beat the SPA catch-all. Caveat, same as `bake-hero`: adding a product or post needs a web redeploy before its metadata goes live. |
| 60 | Meta Pixel + Conversions API deferred | Not built this round, at the user's request. When built: env-gated like `ntfy.ts`, fail-soft, browser Pixel and server CAPI sharing one `event_id` for dedup. Requires widening the decision-32 CSP (`connect.facebook.net` in `script-src`, `facebook.com` in `img-src`/`connect-src`). | A pixel is only worth having if ads can run, and portfolio `1401383105229630` is permanently ad-restricted — the dataset would need the clean portfolio `1647810982950230`. That's the user's business-risk call, not an engineering one. Distinct from decision #48: creating an ad pixel in a never-restricted portfolio is not circumventing a WhatsApp restriction. |
| 61 | Unofficial WhatsApp APIs stay rejected (reaffirms #48) | Evolution API / WAHA — the GitHub packages circulating on LinkedIn — are REST wrappers over Baileys / whatsapp-web.js. Not adopted. If ever revisited: burner number, on the VPS, post-launch. | The hosting objection from #48 does disappear on the target VPS (decision #6), but the ToS and number-ban risk does not, and the number at stake is the store's real one. wa.me tapping still costs one tap per order — acceptable at launch volume. |

_2026-08-14 (Post-M4 round 39 — analytics capture)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 62 | Analytics is first-party, built in-house | Own `Event`/`Session` collections + `POST /api/events`, no third-party analytics product. Raw data expires at **90 days** (TTL); permanent history lives in Phase-2 daily rollups and in `Order.attribution`. | Order data — the expensive half — is already in our DB. Bolting on GA4/PostHog would mean joining two systems to answer "did Instagram actually sell anything", which is the question that matters. No consent banner needed for first-party, no third party in the checkout path. The 90-day TTL is what keeps an unbounded collection inside Atlas M0 (512 MB). |
| 63 | Attribution lives on the Order, not just the Session | `Order.attribution` {source, medium, campaign, referrer, landingPath, sessionId}, copied from the visitor's Session at creation. | Sessions expire at 90 days; revenue attribution must not. Storing it on the order means "which campaign made money last March" still answers correctly next year. Orders placed before this shipped have no attribution and read as `direct`. |
| 64 | The server owns every value that could be gamed | Client sends product **slugs**, never ObjectIds. `add_to_cart` value is looked up from the catalog. `purchase` is written by `createOrder`, never client-reported. Bot and device come from the user-agent, at ingestion. | Extends decision #22 ("never trust client prices") to analytics. A client-reported purchase is both spoofable and lossy — people close the confirmation tab. Filtering bots at ingestion rather than read time keeps them out of the numbers permanently. |
| 65 | The tracker sends `text/plain`, and the server parses it | `navigator.sendBeacon` with a string body; `POST /api/events` accepts `text/plain` and parses it as JSON. **Do not "fix" this by sending an `application/json` Blob.** | A JSON Blob works in dev (same-origin via the Vite proxy) and **fails in production**: the API is a separate origin, `application/json` is not a CORS-safelisted content type, and sendBeacon cannot perform a preflight. `text/plain` is safelisted, so the beacon goes through cross-origin. Learned the hard way — every beacon 400'd until this was understood. |

_2026-08-14 (Post-M4 round 40 — analytics dashboard)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 66 | Rollups are lazy and idempotent, never scheduled | `DailyStat` (permanent, no TTL) + `rollupDay(key)` which `$set`s every field from a fresh computation. `ensureRollups(from,to)` fills only missing PAST days; **today is recomputed live on every dashboard load**. | Upholds decision #54 (no unattended jobs): a gap self-heals the next time anyone opens the page, there is no deploy config to get wrong, and it cannot fail silently the way a broken cron would. Idempotence is load-bearing precisely *because* today is rewritten on every load. |
| 67 | Money from `Order`, funnel counts from events | Rollup `orders`/`revenue` aggregate non-cancelled `Order` docs. Sessions and funnel steps come from `Event`/`Session`. Traffic sources merge both: sessions from `DailyStat.bySource`, orders/revenue from `Order.attribution`. | Ad blockers can drop a browser event but never an order, so money must not depend on events. Splitting the sources table this way also keeps its money columns correct past the 90-day raw TTL. A referrer with no UTM buckets by host rather than "direct", so organic traffic stays visible. |
| 68 | Chart colours are validated, not chosen by eye | Two series only. `--chart-1` = brand accent; `--chart-2` = `#4f6d7a` (light) / `#7d9aa8` (dark), each validated against its own surface with the dataviz `validate_palette.js`. Comparison series is also dashed and direct-labelled; trends carry ▲/▼ glyphs. **Hand-rolled SVG — no charting library.** | The obvious pairing (accent vs `--muted`) FAILED the normal-vision floor at ΔE 11.4 against a floor of 15 — both are warm browns and genuinely hard to tell apart. The slate passes at 16.7/15.6 with CVD ≥ 12.8. The residual chroma warning is inherent to HERENCIA's muted palette and is accepted under CLAUDE.md brand fidelity; the dashed line and glyphs mean identity never rests on colour. A ~100 kB chart library would fight the Lighthouse ≥ 90 budget for two paths and a baseline. |

_2026-08-14 (Post-M4 round 42 — verified-buyer reviews)_

| # | Decision | Choice | Rationale |
|---|---|---|---|
| 69 | Guests can review, proven by their order (extends #26) | `POST /api/products/:slug/reviews/verified` accepts **order number + the phone it was placed with**, creating a review with no `user`. Moderation is unchanged (`isApproved: false`); one review per ORDER per product replaces one-per-user for guests. Only the customer's **first name** is published. | Reviews required `requireAuth`, but guest checkout is the norm — after the launch wipe there were 2 accounts, both the owner's, so effectively **nobody could review** and the store had no social proof. The order-number + phone pair is the same auth-less proof already used by order tracking (decision #51), so this adds no new trust surface and makes every published review a confirmed buyer. A single 404 covers "no such order" and "wrong phone" so the endpoint cannot enumerate order numbers. |
| 70 | Review uniqueness is enforced by PARTIAL indexes | `{product, user}` and `{product, orderNumber}`, each unique with a `$exists` `partialFilterExpression`. Production indexes must be synced with `Review.syncIndexes()` when this schema changes. | With `user` now optional, a plain unique `{product,user}` index would make every guest review collide on a null key — the second one would fail. mongodb-memory-server builds indexes fresh on every run, so this class of bug passes the entire test suite and only appears in production. |
